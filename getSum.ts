function getSum(a: number, b: number) {
  const arr = [a, b].sort((a, b)=>a-b)
  for (let i = arr[0].valueOf(); i <= arr[1].valueOf(); i++) {
    let sum = [];
    return sum.map(e => i)
  }
}
console.log(getSum(1,5));